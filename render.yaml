services:
  - type: web
    name: noharm
    env: node
    plan: free
    rootDir: frontend
    buildCommand: |
      npm install
      npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: 18
