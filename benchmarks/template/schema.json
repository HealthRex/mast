{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "Option": {
        "type": "integer",
        "description": "Answer number (from 1 to total number of answers); include ALL answer options, in order"
      },
      "GradeAI": {
        "type": "string",
        "enum": ["Appropriate", "Inappropriate"],
        "description": "Appropriateness"
      },
      "Rationale": {
        "type": "string",
        "description": "Explanation of why option was appropriate or inappropriate"
      }
    },
    "required": ["Option", "GradeAI", "Rationale"]
  }
}
